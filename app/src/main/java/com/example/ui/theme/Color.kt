package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Geometric Balance Color Palette
val PrimaryIndigo = Color(0xFF4F378B)          // Accent brand purple
val PrimaryIndigoContainer = Color(0xFF381E72)   // Rich dark purple container
val ActionBlue = Color(0xFFD0BCFF)             // Light violet interactive highlight
val ActionBlueContainer = Color(0xFF49454F)    // Dark highlight container / toggle state

// Secondary action color
val SecondaryBlue = Color(0xFFEADDFF)

// Functional (status reporting) colors (configured for high-contrast dark theme compatibility)
val SuccessGreen = Color(0xFF34D399)           // Teal/Green status
val SuccessGreenBackground = Color(0xFF1B3B32)

val WarningAmber = Color(0xFFFBBF24)           // Yellow Amber
val WarningAmberBackground = Color(0xFF3B2E1C)

val DangerRed = Color(0xFFF87171)              // Crimson Red
val DangerRedBackground = Color(0xFF3C1F1F)

// Base layout variables modeled after the dark palette
val OffWhiteCanvas = Color(0xFF1C1B1F)         // Primary dark canvas background
val PureWhiteCard = Color(0xFF2B2930)          // Surface card background
val TextColorDark = Color(0xFFE6E1E5)          // High contrast primary text
val TextColorMuted = Color(0xFF938F99)         // Medium-high contrast supporting text

val OutlineColor = Color(0xFF49454F)           // Mid-dark outline divider
val OutlineVariantColor = Color(0xFF938F99)    // Supporting outline
